class Solution {
public:
    int maxCount(int m, int n, vector<vector<int>>& ops) {
        if(ops.size() == 0) return m*n;

        int r = m, c = n;
        for(auto x : ops){
            r = min(r,x[0]);
            c = min(c,x[1]);
        }
        return r*c;
    }
};
